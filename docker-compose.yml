services:
  app:
    build:
      context: ./app
    ports:
      - "5174:80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80"]
      interval: 5s
      timeout: 3s
      retries: 10
